meta {
  name: Add tree to a forest
  type: http
  seq: 4
}

post {
  url: http://localhost:3000/forest/{{ForestId}}/tree
  body: json
  auth: inherit
}

body:json {
  {
    "treeId": "{{treeId2}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
