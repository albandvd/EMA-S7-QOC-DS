meta {
  name: Get all forests
  type: http
  seq: 2
}

get {
  url: http://localhost:3000/forest
  body: none
  auth: none
}

tests {
  test("All forest received", () => {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
    
    res.body.forEach(forest => {
      expect(forest).to.have.property("type");
      expect(forest).to.have.property("surface");
      expect(forest).to.have.property("trees");
    });
  });
}
