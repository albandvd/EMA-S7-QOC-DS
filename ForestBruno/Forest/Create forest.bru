meta {
  name: Create forest
  type: http
  seq: 1
}

post {
  url: http://localhost:3000/forest
  body: json
  auth: none
}

body:json {
  {
    "type": "BOREAL",
    "surface": 1000,
    "trees": []
  }
}

tests {
  test("Forest created", () => {
    expect(res.status).to.equal(201)
  
    const body = res.body
    expect(body).to.have.property("id")
    const ForestId = body.id
    bru.setVar("ForestId", ForestId)
  })
}
