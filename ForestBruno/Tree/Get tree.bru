meta {
  name: Get tree
  type: http
  seq: 4
}

get {
  url: http://localhost:3000/tree/{{treeId1}}
  body: none
  auth: none
}

tests {
  test("Tree received", () => {
    expect(res.status).to.equal(200)
    expect(res.body).to.have.property("species", "OAK")
    expect(res.body).to.have.property("exposure", "SUNNY")
  })
  
}
