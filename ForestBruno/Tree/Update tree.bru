meta {
  name: Update tree
  type: http
  seq: 5
}

put {
  url: http://localhost:3000/tree/{{treeId1}}
  body: json
  auth: none
}

body:json {
  {
    "birth": "2024-01-01T00:00:00.000Z",
    "species": "BEECH",
    "exposure": "SHADOW",
    "carbonStorageCapacity": 150
  }
}

tests {
  test("Tree received", () => {
    expect(res.status).to.equal(200)
    expect(res.body).to.have.property("species", "BEECH")
    expect(res.body).to.have.property("exposure", "SHADOW")
  })
  
}
